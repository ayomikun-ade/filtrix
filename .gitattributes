# Normalize line endings: store LF in the repo, check out LF everywhere.
* text=auto eol=lf

# Treat known binaries as binary (no EOL conversion).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
